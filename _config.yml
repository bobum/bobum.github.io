# _config.yml
title: "Scott Davis"
tagline: "Essays on Engineering Leadership and Generative Storytelling"
description: "Scott Davis — Tech leader scaling teams with Copilot/Cursor, while writing AI-aided fiction (Sudowrite/Claude). Essays on engineering leadership *and* generative storytelling." # Helps SEO
baseurl: ""                   # Critical for GitHub Pages
url: "https://davisplanet.com" # Your live URL

collections:
  # This defines your fiction collection
  fiction:
    output: true # This is CRITICAL. It tells Jekyll to create a separate page for each item.
    permalink: /fiction/:title/ # This sets the beautiful, SEO-friendly URL structure.

permalink: /blog/:title/
excerpt_separator: "<!--more-->"

# Liquid filter (replaces plugin)
liquid:
  filters:
    reading_time: |
      def reading_time(input)
        words = input.to_s.gsub(/<[^>]*>/, '').split.size # Strip HTML tags
        minutes = (words / 180.0).ceil
        "#{minutes} min read"
      end

plugins:
  - jekyll-seo-tag  # Auto-generates meta tags

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
